"use client";

import { useSearchParams } from "next/navigation";

export default function CalendarCallbackClient() {
  const sp = useSearchParams();
  // ...tu lógica actual (leer code, returnTo, etc.)
  return <div>Procesando OAuth...</div>;
}
